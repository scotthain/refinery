name: Release the refinery binaries
on:
  push:
    branches:
      - 'shain/**'
jobs:
  go-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Go build
        run: echo FUUUUPPPPPMFFF
